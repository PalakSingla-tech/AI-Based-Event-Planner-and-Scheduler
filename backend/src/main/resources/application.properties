spring.application.name=AuroraEvents
server.port=8080

# ========== DATABASE CONFIG ==========
spring.datasource.url=jdbc:mysql://localhost:3306/eventMate
spring.datasource.username=root
spring.datasource.password=2830@412Ps

# JDBC Driver (for MySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== JPA / HIBERNATE CONFIG ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Allow frontend running on port 5173 (for Vite/React)
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# ========== AI CONFIG (Google Gemini) ==========
gemini.api.key=AIzaSyDCUtXlak6aR_JP0m3eUtRAPV21SYcgoto

# ========== EMAIL / SMTP CONFIG ==========
# SMTP Server Configuration (Gmail example - replace with your credentials)
# NOTE: SMTP is currently DISABLED - emails will be logged to console only
# Uncomment and configure these lines when you want to send real emails
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=your-email@gmail.com
#spring.mail.password=your-app-password
# SMTP Properties
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.smtp.starttls.required=true
#spring.mail.properties.mail.smtp.connectiontimeout=5000
#spring.mail.properties.mail.smtp.timeout=5000
#spring.mail.properties.mail.smtp.writetimeout=5000

# ========== BOOKING REMINDER CONFIG ==========
# Number of days in advance to send reminders
booking.reminder.days.advance=7
# Cron expression: Daily at 9 AM (second minute hour day month weekday)
booking.reminder.cron=0 0 9 * * ?